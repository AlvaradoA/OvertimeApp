
<nav class="custom-nav navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Time Tracker", root_path, class: "navbar-brand", id: "logo" %>
    </div>
    <% if current_user %>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li class ="<%= active?(root_path) %>"><%= link_to " ", root_path, class: "glyphicon glyphicon-home" %></li>
          <%= render 'shared/audit_log_tab' if policy(AuditLog).index? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Overtime Entries<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li class ="<%= active?(posts_path) %>"><%= link_to "Your Overtime Entries", posts_path %></li>
              <li role="separator" class="divider"></li>
              <li class ="<%= active?(new_post_path) %>"><%= link_to "Add New Entry", new_post_path, id: "new_post_from_nav" %></li>
            </ul>
          </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-cog"><span class="caret"></span></a>
              <ul class="dropdown-menu">
                <%= render 'shared/admin_dashboard_tab' if admin_types.include?(current_user.try(:type)) %>
                <li><%= link_to "Log Out", destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
          </ul>
      </div>
    <% end %>
  </div>
</nav>
